# Open Questions

Every `Q-` entry defined in the corpus, compiled by script from the spec files. Each names the spec section it blocks and asks something only the product owner can answer. Entries marked resolved in their source spec are kept for the record.

Total entries: 117

| ID | Defined in | Entry |
|---|---|---|
| Q-001 | `10-architecture/data-model/schedule.md` | Blocks: field table enum column. Whether the platform accepts or rejects `source_type` values outside the declared enum (`event`, `goal`) at write time is not visible in the repository. |
| Q-002 | `10-architecture/data-model/flags-and-lifecycle.md` | Blocks: every "Default" column. Whether the platform applies schema defaults when a create omits the field (e.g. `ScheduleItem.completed`, `Chore.status` on rows created without `status`) is not visible. |
| Q-003 | `10-architecture/data-model/tasks.md` | Blocks: field table "Default" semantics. Several writers send `null` for declared fields (`due_date`, `recurrence_pattern`, `occurrences`, `completed_count`, `schedule_time`); whether the platform stores `null`, drops the key, or applies the schema default is not visible in the repository. |
| Q-004 | `10-architecture/data-model/vision-board.md` | Blocks: `CollageImage` seed-set description. The seed source is every `is_default: true` row from any owner (`base44/functions/initializeDefaultCollageImages/entry.ts:19-20`, comment "builder's marked default images"); which owner's rows are intended as the seed set is not visible in the repository. |
| Q-005 | `10-architecture/data-model/sync.md` | Blocks: `DeletedSyncItem` lifecycle "created". No function or component creates tombstone rows; the review UI, the entity, and the full-wipe list reference them. Whether a producer exists outside the repository is not visible. |
| Q-006 | `10-architecture/data-model/settings.md` | Blocks: `ReminderSettings` lifecycle. No create or update path exists in the repository; the dashboard reads the first row. Whether rows are created outside the repository is not visible. |
| Q-007 | `10-architecture/data-model/schedule.md` | Blocks: `relationships.md` §5 cascade table. The subscription callback is read as `event.type === 'delete'` with `event.id` (`src/pages/DailySchedule.jsx:169`); the platform's event shape is not documented in the repository. |
| Q-008 | `10-architecture/data-model/settings.md` | Blocks: `User` field table. `email`, `full_name`, `id`, and the two acceptance flags are used but not declared in `base44/entities/User.jsonc`; which are platform built-ins and which are free-form attributes accepted by `auth.updateMe` is not visible in the repository. |
| Q-100 | `10-architecture/data-model/seed-data.md` | The actual URLs of the builder-marked default images are not in the repo; see Open questions. |
| Q-102 | `10-architecture/preferences.md` | No UI writes `task_sync_category` / `task_sync_color`; the manual describes a Settings control. Blocks A.2 rows. |
| Q-103 | `10-architecture/external-services.md` | OAuth scopes are configured on the platform connector, not in the repo; the table lists the permissions the endpoints require. Blocks §3.2 "Implied permission" column. |
| Q-106 | `10-architecture/time-and-date-semantics.md` | Under what condition does the Quotes rollover check (AR-TIME-43) observe two different strings for the same instant? Both operands are computed from the device clock in local time. Blocks §5. |
| Q-107 | `10-architecture/time-and-date-semantics.md` | What timezone does the function runtime apply to `toLocaleDateString('en-CA')` (formatter D)? The source comment asserts UTC. Blocks §6. |
| Q-200 | `10-architecture/google-sync.md` | Blocks §2a. Which connector-id pair is live in the deployed app, and are the sign-up and terms-acceptance ids stale aliases or separate connectors? |
| Q-201 | `10-architecture/google-sync.md` | Blocks §9. Which sync run is intended to write `pending_review` tombstones, and for which source types (the entity allows `task`, the review panel restores only `calendar`)? |
| Q-202 | `10-architecture/google-sync.md` | Blocks §3. Are per-user `sync_times` and `auto_sync_calendar_ids` intended to drive scheduling and calendar scope for the scheduled import? |
| Q-203 | `10-architecture/schedule-hub.md` | Blocks §3b. Which flow is intended to create `chore` and `education` schedule items (the User Manual's "auto-population")? |
| Q-204 | `10-architecture/automations.md` | Blocks §2. Under which identity does the platform execute scheduled workflows? The quote function's comment asserts the app owner (`base44/functions/generateDailyQuotes/entry.ts:10-11`); the other two scheduled functions have no such guard. |
| Q-205 | `10-architecture/automations.md` | Blocks §2c. Is the daily sync intended to fan out per user (with each user's `sync_times`), or to be a single owner-scoped run? |
| Q-206 | `10-architecture/schedule-hub.md` | Blocks §8b. Is the backend `deleteToDoItem` intended to be the single path for all four actions, or is the client-side path for three of them the intended design? |
| Q-207 | `10-architecture/google-sync.md` | Blocks §7. Is `syncTasksToCalendar` a live feature awaiting a UI entry point, or superseded? |
| Q-208 | `10-architecture/automations.md` | Blocks §3. Is the PATCH on every update of a pushed custom block (AR-AUTO-21) the intended behaviour, or is the push meant to follow field edits only? |
| Q-209 | `10-architecture/admin-operations.md` | Blocks §2.4. Is `ChoreLibrary.assigned_to` a field from an earlier schema that rows may still carry, which this function exists to clear? |
| Q-210 | `10-architecture/admin-operations.md` | Blocks §1. Are the four functions without an app entry point (§AR-ADMIN-01) meant to be run from the platform console only, or is an admin UI planned? |
| Q-211 | `10-architecture/admin-operations.md` | Blocks §2.8. Is the shorter entity list in account deletion intended (for example to keep shared seed images), or is the 25-entity list the intended scope? (Both lists are implemented as cited.) |
| Q-300 | `10-architecture/ai-services.md` | (§8) Is the marketing claim "AI-generated quote" meant to describe the fallback only, or is the external quote source incidental? |
| Q-301 | `10-architecture/ai-services.md` | (§2c) Which default is intended for "Save selected to library for future use"? |
| Q-310 | `10-architecture/export-print-email.md` | (§2, Tier A) Is generic print/email intended to be surfaced on every widget card, or only on the surfaces that bind it inline? |
| Q-311 | `10-architecture/export-print-email.md` | (§3, Goals) Which Goals export is intended as the product behaviour: per-timeframe card export, the unbound page-level builder, or both? |
| Q-312 | `10-architecture/export-print-email.md` | (§3, Tasks) Is the Tasks page export intended to honour the label filter that is applied on screen? |
| Q-320 | `10-architecture/shared-interactions.md` | (§3) Is one double-tap window intended across rows? |
| Q-321 | `10-architecture/shared-interactions.md` | (§10) Is the desktop sidebar collapsed state intended to persist across sessions? |
| Q-330 | `10-architecture/auth-and-account.md` | (§2, §5, §10) Which connector id pair represents the product's Google Calendar and Google Tasks connection? |
| Q-331 | `10-architecture/auth-and-account.md` | (§10) Is a visible sign-out intended? |
| Q-332 | `10-architecture/auth-and-account.md` | (§11) Is MCP client access part of the product, and at which path is the consent page meant to be reachable? |
| Q-333 | `10-architecture/auth-and-account.md` | (§10) Is account deletion intended to cover every entity the account owns? |
| Q-400 | `20-features/daily-schedule/spec.md` | Blocks §5b. Which `AR-TIME-nn` rules cover this page's time-of-day-aware overdue test (`src/pages/DailySchedule.jsx:501-516`) and the UTC-string date stepping (D-409)? Pending `10-architecture/time-and-date-semantics.md`. |
| Q-401 | `20-features/daily-schedule/spec.md` | Blocks `time-grid.md` §"Move to now". A carry-over pseudo-item can satisfy the past-item test and offer "↻ Move to now" and "Edit"; the update then targets the pseudo id `carryover-<id>` (`src/pages/DailySchedule.jsx:310,367-382,727,782-787`). What is the observed outcome? |
| Q-402 | `20-features/daily-schedule/spec.md` | Blocks `daily-todo.md` §completion. Completing a chore-sourced row writes `Chore.status` only (`src/components/DailyToDo.jsx:123-124`). Does the Chores feature's own completion path advance the next due date, and is that divergence intended? For the chores writer. |
| Q-403 | `20-features/daily-schedule/spec.md` | Blocks §"Sources referenced". Is `base44/functions/deleteToDoItem/entry.ts` owned by this feature or by `10-architecture/google-sync.md`? Its non-Google actions duplicate the to-do component's own logic (D-411). |
| Q-404 | `20-features/daily-schedule/spec.md` | Blocks `item-library.md` §Goals. When a goal has no incomplete milestone tasks, the goal itself is offered and the resulting schedule item's `source_id` is the goal id with source type `goal` (`src/pages/DailySchedule.jsx:1054-1056,1099`); the to-do's goal rows have no checkbox and completion routes to `GoalTask` (`src/components/DailyToDo.jsx:108,127-128`). Is scheduling the goal itself intended? |
| Q-405 | `20-features/daily-schedule/spec.md` | Blocks §4 Active hours. With End earlier than Start the hour list is empty and the grid renders no rows (`src/pages/DailySchedule.jsx:137`). Is a validation rule intended? |
| Q-450 | `20-features/tasks/spec.md` | Blocks: §4.10. Question: is the realtime subscription intended to refresh the visible list (no consumer of the invalidated query key is present on the page — `src/pages/Tasks.jsx:125-138`)? |
| Q-451 | `20-features/tasks/spec.md` | Blocks: §4.8. Question: is the unrendered CategoryFilter control (`src/pages/Tasks.jsx:27,49,596-599`) intended to appear on the Tasks page, and if so where relative to the Sort/Label controls? |
| Q-452 | `20-features/tasks/spec.md` | Blocks: §4.6, §4.7. Question: is batch delete intended to write trash snapshots and offer the Google choice like single delete (`src/pages/Tasks.jsx:331-337` vs `:347-367`)? |
| Q-453 | `20-features/tasks/spec.md` | Blocks: §5a. Question: is the `in_progress` status meant to be reachable from the Tasks page as the manual describes (D-453)? |
| Q-455 | `20-features/tasks/recurrence.md` | Blocks: §4. Question: which view, if any, is the "completed this period" helper (`src/pages/Tasks.jsx:145-165`) meant to drive? |
| Q-456 | `20-features/tasks/recurrence.md` | Blocks: §3. Question: is the sibling check intended to match on title and pattern only, so that two distinct recurring tasks sharing a title suppress each other's next occurrence (`src/pages/Tasks.jsx:260-267`)? |
| Q-457 | `20-features/tasks/recurrence.md` | Blocks: §6. Question: is the dashboard widget meant to surface `biweekly` and `days_of_week` tasks on their scheduled days (D-471)? |
| Q-460 | `20-features/tasks/google-tasks.md` | Blocks: §4. Question: what is "Delete from Google too" intended to do in Google (delete, or mark completed), and which Google list is meant to be addressed, given `updateGoogleTask` uses the default list (`base44/functions/updateGoogleTask/entry.ts:35`)? |
| Q-461 | `20-features/tasks/google-tasks.md` | Blocks: §2d. Question: on an import update, is the local label and colour of an already-imported task meant to be replaced with the default sync label each time (`base44/functions/syncGoogleTasks/entry.ts:64-69`)? |
| Q-462 | `20-features/tasks/google-tasks.md` | Blocks: §5b. Question: is the intended retention window 24 hours or 30 days (D-486), and are trash rows older than the window meant to be removed? |
| Q-463 | `20-features/tasks/google-tasks.md` | Blocks: §5b. Question: is Restore meant to re-link schedule items that referenced the deleted task's old id (`src/pages/Settings.jsx:203-204`)? |
| Q-500 | `20-features/calendar/spec.md` | Blocks: `deleted-item-review.md` §"When the banner appears". Question: no code path in `src/` or `base44/functions/` creates a `DeletedSyncItem` or sets `status: pending_review`; what is intended to populate the review queue? |
| Q-501 | `20-features/calendar/spec.md` | Blocks: §4 "Edit event (from Daily Schedule and Daily To-Do)". Question: the "Apply permanent changes to Google Calendar" checkbox takes its value once at component mount (`src/components/EventEditDialog.jsx:20`) and is not recomputed per event; what is the intended default for a linked event opened later? |
| Q-502 | `20-features/calendar/spec.md` | Blocks: §9. Question: which of the two walkthrough step sets (D-505) is the intended copy? |
| Q-503 | `20-features/calendar/spec.md` | Blocks: §4 "Edit event (from the Calendar page)". Question: the Calendar's Edit Event form carries notes but shows no Notes field (`src/components/SwipeableEventItem.jsx:26-32,186-219`); is notes editing intended from this page? |
| Q-504 | `20-features/calendar/spec.md` | Blocks: §1. Question: the landing page claims Google Drive sync (`src/pages/LandingPage.jsx:12`); no Drive behaviour is observed in this feature's sources — which spec owns that claim? |
| Q-505 | `20-features/calendar/deleted-item-review.md` | Blocks: §6 BR-CAL-25. Question: how are `allowed` and `denied` tombstones meant to influence a later import (the entity says "whether to allow re-sync"), given no reader of `status` beyond the banner is observed? |
| Q-506 | `20-features/calendar/deleted-item-review.md` | Blocks: §5. Question: the restore has no original date or times available on the tombstone; is landing on the current date at 09:00–10:00 the intended outcome, or is a later import expected to correct it? |
| Q-550 | `20-features/chores/spec.md` | Blocks §4.1. Is a household member named `UNASSIGNED` expected to exist? The page filters that name from every list (`src/pages/Chores.jsx:186`) but no owned file creates it; the generator's unassigned choice writes `assigned_to: ""` (`src/components/ChoreGenerator.jsx:164`, per `ai-generator.md`). |
| Q-551 | `20-features/chores/spec.md` | Blocks §4.5, §4.6. Is a category filter intended? The category grouping branch and the `filterCategory` state exist (`src/pages/Chores.jsx:44,1072-1088`) with no control that sets a value; only "Clear all" touches it (`:1398`). |
| Q-552 | `20-features/chores/spec.md` | Blocks §4.3. The edit dialog's Room input references a suggestion list `room-suggestions` (`src/pages/Chores.jsx:1523`) that is not rendered; is the room list meant to be offered as suggestions there as it is in the add dialog (`:1201-1213`)? |
| Q-553 | `20-features/chores/spec.md` | Blocks §5 BR-CHORE-01. Chores with an empty `assigned_to` (as the generator can create) never appear on the Chores tab (`src/pages/Chores.jsx:577`); is there an intended surface for them? |
| Q-554 | `20-features/chores/spec.md` | Blocks §9. The page passes an `onDontRemind` handler to the walkthrough (`src/pages/Chores.jsx:1254`) that the dialog never invokes (`src/components/onboarding/ChoresOnboarding.jsx:33-37`); is a second dismissal path intended? |
| Q-555 | `20-features/chores/chore-library.md` | Blocks §4.2. The age and assignee filters exist as state and rules (`src/components/ChoreLibraryDialog.jsx:32-33,95-96`) with no control; are they intended to be exposed, and with which options? |
| Q-556 | `20-features/chores/chore-library.md` | Blocks §4.2, §11. Is the declared seven-value chore-type list (`src/components/ChoreLibraryDialog.jsx:13`) intended as a Type filter or as the vocabulary for `chore_type`? |
| Q-557 | `20-features/chores/chore-library.md` | Blocks §4.4. What is the intended effect of deleting a "My Chore" entry: removing the user's chore, hiding it from the library view, or nothing? Today it sends a library delete for a `Chore` id (`src/components/ChoreLibraryDialog.jsx:184,378`). |
| Q-600 | `20-features/chores/meal-planning.md` | Blocks §4 "Editing a meal". Meals cannot be opened for edit on either tab; notes and directions can be entered only in the chore form at creation. Is a meal edit path intended? |
| Q-601 | `20-features/goals/spec.md` | Blocks §4a. The occurrence-editing pill offers "−", "+", and "✓" only (`src/pages/Goals.jsx:456-473`); is a cancel gesture (Escape or outside click) intended? |
| Q-602 | `20-features/goals/spec.md` | Blocks §5a. Creating a goal with milestone lines does not run the derivation (`src/pages/Goals.jsx:260-279`), so a new goal keeps entity defaults until a milestone changes. Intended? |
| Q-603 | `20-features/goals/spec.md` | Blocks §4 dashboard widget. A timeframe bucket whose goals fail BR-GOAL-14 still renders with "(0)" (`src/components/dashboard/DashboardGoals.jsx:32,42`). Intended? |
| Q-604 | `20-features/chores/meal-planning.md` | Blocks §4b. The Menu tab's member filter is the same state as the Chores tab's member filter (`src/pages/Chores.jsx:42,1315,1417`), so selecting a member on one tab filters the other. Intended? |
| Q-650 | `20-features/education/spec.md` | Blocks §4.3. Is a plan created while no learners exist (learner id = the user's full name) meant to be displayed, and under which learner? |
| Q-651 | `20-features/education/spec.md` | Blocks §4.5 / BR-EDU-06. When "All Learners" is shown and the opened card belongs to a learner other than the active learner, which learner's plans is "Delete Subject" meant to remove? |
| Q-652 | `20-features/education/spec.md` | Blocks `ai-activities.md` §4 and `activity-library.md` §4. Which of the two notes formats (D-657) is the intended one for activities created from suggestions and library entries? |
| Q-653 | `20-features/education/spec.md` | Blocks §7. Which flow is intended to create Daily Schedule items of source type `education` ("Add to Schedule", "synced to the Daily Schedule")? Same question as Q-203. |
| Q-654 | `20-features/education/spec.md` | Blocks §12. Is the per-learner × subject layout in `PrintFormatEducation` intended as a second export alongside the weekly schedule, or as its replacement? |
| Q-700 | `20-features/vision-board/spec.md` | Blocks §4 / `health-pillars.md` §6. The page defines `createGoalFromPillar` (title `{pillar} Goal`, description `Created from Vision Board - {pillar} pillar`, timeframe `monthly`, alert `"{pillar}" goal created! View it in Goal Manager.`) and passes it to the pillar manager as `onCreateGoal`, which never calls it (`src/pages/VisionBoard.jsx:157-173,233`, `src/components/visionboard/PillarManager.jsx:26`). Is a per-pillar "create goal" control intended? |
| Q-701 | `20-features/vision-board/spec.md` | Blocks `health-pillars.md` §1. On the very first visit the in-memory pillar list after seeding is the default array, which has no `id` values until the page is reloaded (`src/pages/VisionBoard.jsx:82-87`); activities, hide, edit and goal creation key on `pillar.id`. What does the account owner observe on the Pillars tab during that first visit? |
| Q-702 | `20-features/vision-board/spec.md` | Blocks §7a. `ReminderSettings` has no writer. Where is the account owner intended to set `enabled` and `times`, and what is a reminder intended to do beyond the dashboard highlight? |
| Q-703 | `20-features/vision-board/spec.md` | Blocks `daily-evaluation.md` §4. In edit mode the "Cancel" button appears only on the gratitude step (`src/components/visionboard/DailyEvaluation.jsx:549-555`). Is leaving edit mode from an earlier step intended to require stepping through to the end? |
| Q-750 | `20-features/vision-board/collage.md` | Blocks: §4.4. `saveCurrentImagesAsDefaults` is defined with an alert and a busy flag but no control invokes it (`src/pages/VisionBoard.jsx:177-206`); is a "Save current images as defaults" control intended on the Collage tab, and should it act on the picker list or on the gallery? |
| Q-751 | `20-features/vision-board/slideshow.md` | Blocks: §4.3 Previous. Pressing Previous (or swiping right) before any forward step moves the affirmation position below zero (`src/components/visionboard/Slideshow.jsx:457-460`), and the queue lookup uses that position directly (`:319-326`). What the overlay shows in that case could not be determined by reading. |
| Q-752 | `20-features/vision-board/slideshow.md` | Blocks: §4.1. The preparer passes the pillar list as `focusAreas` (`src/components/dashboard/DashboardSlideshow.jsx:150`) and the Collage-tab Custom launch passes an empty list (`src/pages/VisionBoard.jsx:458`); the player does not read the prop (`src/components/visionboard/Slideshow.jsx:41`). What was the prop intended to drive? |
| Q-753 | `20-features/vision-board/slideshow.md` | Blocks: §6. Shared images for both preparer modes are read with `CollageImage.list()` and no limit (`src/components/dashboard/DashboardSlideshow.jsx:16`); the platform's default page size, and therefore the maximum number of shared images a show can contain, is not in the repository. |
| Q-754 | `20-features/vision-board/collage.md` | Blocks: §4a. The eye, trash, and star controls appear on pointer hover only (`src/components/visionboard/ImageGallery.jsx:78`; `src/components/visionboard/PrivateImageUploader.jsx:192`; `src/pages/VisionBoard.jsx:407`); the intended touch interaction for reaching them is not determinable from the source. |
| Q-800 | `20-features/dashboard/spec.md` | Blocks: §4.3 order merge. Question: what is the intended behaviour when `ThemeSettings.widget_order` contains an id that is not in the registry (for example after a widget is removed)? The page looks the id up in the registry with no fallback (`src/pages/Dashboard.jsx:252-254`). |
| Q-801 | `20-features/dashboard/spec.md` | Blocks: §4.2 Focal Areas binding. Question: the page stores the Focal Areas highlight callback value but nothing reads it (`src/pages/Dashboard.jsx:56,311-313`). What page-level cue was intended? |
| Q-802 | `20-features/dashboard/spec.md` | Blocks: §4.1. Question: a long-form date string is computed on every render and not displayed (`src/pages/Dashboard.jsx:153`). Where was it meant to appear? |
| Q-803 | `20-features/daily-checklist/spec.md` | Resolved in synthesis: `10-architecture/data-model/checklist.md` D-012 now records the dashboard widget sort as label then `order` (`src/components/dashboard/DashboardChecklist.jsx:71-78`), matching BR-CHK-03. No open question remains. |
| Q-804 | `20-features/daily-checklist/spec.md` | Blocks: §4.10. Question: the widget computes whether any pending item sits outside Anytime and never uses the value (`src/components/dashboard/DashboardChecklist.jsx:63`). Was a flat, un-bucketed layout intended when everything is Anytime? |
| Q-850 | `20-features/settings/spec.md` | Blocks §4.6. Is the sync dialog intended to stay open until every ticked source has finished (D-850)? |
| Q-851 | `20-features/settings/spec.md` | Blocks §4.9, `data-management.md` §2. Is "Delete Synced Data" intended to remove in-app events and custom blocks along with imported calendar rows (D-853)? |
| Q-852 | `20-features/settings/spec.md` | Blocks §0, §4.5. Are the two sync cards intended to remain reachable while both connectors are disconnected, as they are whenever saved calendar or task-list rows exist (`src/pages/Settings.jsx:864`)? |
| Q-853 | `20-features/settings/spec.md` | Blocks §4.2. Is the Save button on the Dashboard card intended to govern the feature toggles as well, given each switch already saves on click (D-854)? |
| Q-854 | `20-features/settings/trash-bin.md` | Blocks §2. Is the trash intended to grow to other item types (the entity name and `item_type` field are generic, the enum holds only `task`, and Restore branches on `item_type`)? `base44/entities/TrashBin.jsonc:5-11`, `src/pages/Settings.jsx:201` |
| Q-855 | `20-features/settings/data-management.md` | Blocks §4. When `deleteSyncedData` deletes a `custom` schedule item that carries a `google_event_id`, is the entity-triggered delete workflow intended to fire and remove the Google event, contrary to "Your Google data remains unchanged."? No evidence in the repository shows whether function-initiated deletes trigger the workflow (`base44/workflows/Sync App Events to Google Calendar (Delete).jsonc:5-12`). |
| Q-900 | `20-features/quotes/spec.md` | Blocks: §4 Past Quotes section. The "Delete past quotes" button is styled to appear on hover of a `group` ancestor (`src/pages/Quotes.jsx:315`), and neither `WidgetCard` (`src/components/WidgetCard.jsx:30-41`) nor the page declares one. Is the button meant to be always visible, or revealed on card hover? |
| Q-901 | `20-features/quotes/spec.md` | Blocks: §4 Email. State for an email-address dialog exists and is never rendered (`src/pages/Quotes.jsx:31-33`). Is a recipient prompt intended for quotes, given onboarding step 4 says "to yourself or others"? |
| Q-910 | `20-features/links/spec.md` | Blocks: §4 Card grid. Link deletion from the card is immediate (`src/pages/Links.jsx:185-188,591`) while category deletion confirms (`src/pages/Links.jsx:143`) and the shared list rows always confirm (`shared-interactions.md` AR-UI-02). Is an unconfirmed link delete the intended behaviour? |
| Q-911 | `20-features/links/spec.md` | Blocks: §4 Import. The success message is set in the same step that closes the manage dialog (`src/pages/Links.jsx:213-215`), so it is visible only when the dialog is reopened. Is the message meant to be seen on the page? |
| Q-912 | `20-features/links/spec.md` | Blocks: §4 Edit Category. Renaming a category leaves links on the old name (`src/pages/Links.jsx:137-140`). Is a rename meant to carry the links across? |
| Q-920 | `20-features/theme-editor/spec.md` | Blocks: §4 Action bar. "Set as Default" writes `default_theme` (`src/pages/ThemeEditor.jsx:181`) and nothing reads it; "Reset" restores the hardcoded defaults (`src/pages/ThemeEditor.jsx:186-188`). What is the snapshot meant to restore, and from where? |
| Q-921 | `20-features/theme-editor/spec.md` | Blocks: §4 Action bar, §6. Save Theme spreads every loaded row field back into the update (`src/pages/ThemeEditor.jsx:87-88,163-166`), including fields other pages write (`onboarding_status`, `widget_order`, feature toggles, `dashboard_header`, sync settings). Is the intent to save only the ten theme fields? |
| Q-922 | `20-features/theme-editor/spec.md` | Blocks: §4 "Background Image" card. Removing a library URL deletes matching default collage images without confirmation (`src/pages/ThemeEditor.jsx:149-152,370-373`). Is the shared pool between backgrounds and default collage images intended to be visible to the account owner? |
| Q-950 | `20-features/app-shell/spec.md` | Blocks: §4.11. Is a web-app manifest (installable app name, icons, start URL) part of the product? The document links `/manifest.json` but no manifest exists in the repository. |
| Q-951 | `20-features/app-shell/spec.md` | Blocks: §4.5. Is the fresh-open rule intended to override a deep link opened in a new tab (for example a shared `/visionboard?tab=evaluation` link), which currently lands on `/dashboard`? |
| Q-952 | `20-features/app-shell/spec.md` | Blocks: §4.11. Which of the five names in D-952 is the product name for the document title and the installed-app name? |
| Q-960 | `20-features/onboarding/spec.md` | Blocks: §4.1 Dashboard row. Is the Dashboard walkthrough intended to follow the account (like Tasks) or the device (like Calendar)? |
| Q-961 | `20-features/onboarding/spec.md` | Blocks: §9. For Calendar and Daily Schedule, which of the two step sets is the intended content? |
| Q-962 | `20-features/onboarding/spec.md` | Blocks: §4.2. Is the page-supplied `onDontRemind` handler intended to be a separate "don't remind" action distinct from "Got it", as the Vision Board has, for the five device-only pages? |
| Q-970 | `00-overview/legal-copy.md` | Blocks: §2 (6), §3 (7). Which contact address is intended for the legal pages? (Also blocks `20-features/user-manual` §4.4.) |
| Q-971 | `00-overview/legal-copy.md` | Blocks: §4. Is the terms gate intended to present the full texts (or link to them) rather than the summaries? |
| Q-972 | `00-overview/legal-copy.md` | Blocks: §5 (Retention, Deletion). Are retention and deletion commitments intended to be stated, given that an account-deletion control exists (`10-architecture/auth-and-account.md` §10)? |
| Q-1000 | `00-overview/product-vision.md` | Blocks §4.1 row L8. What does "encrypted storage" refer to: platform-level storage encryption outside the repository, transport encryption, or a commitment not yet backed by anything? |
| Q-1001 | `00-overview/product-vision.md` | Blocks §2.2, §2.3. Are household members and learners intended to remain labels inside one account, or is a shared household with separate logins a planned direction? Every entity is single-tenant today (AR-AUTH-07) and goals reference members by display name (BR-GOAL-04). |

## Also referenced as a definition in

- Q-001: `10-architecture/data-model/schedule.md`, `10-architecture/data-model/flags-and-lifecycle.md`
- Q-003: `10-architecture/data-model/tasks.md`, `10-architecture/data-model/flags-and-lifecycle.md`
- Q-004: `10-architecture/data-model/vision-board.md`, `10-architecture/data-model/flags-and-lifecycle.md`
- Q-005: `10-architecture/data-model/sync.md`, `10-architecture/data-model/flags-and-lifecycle.md`
- Q-006: `10-architecture/data-model/settings.md`, `10-architecture/data-model/flags-and-lifecycle.md`
- Q-007: `10-architecture/data-model/relationships.md`, `10-architecture/data-model/flags-and-lifecycle.md`, `10-architecture/data-model/schedule.md`, `10-architecture/data-model/relationships.md`
- Q-008: `10-architecture/data-model/settings.md`, `10-architecture/data-model/flags-and-lifecycle.md`
- Q-500: `20-features/calendar/spec.md`, `20-features/calendar/deleted-item-review.md`
- Q-601: `20-features/goals/spec.md`, `20-features/goals/milestone-tasks.md`
- Q-851: `20-features/settings/spec.md`, `20-features/settings/data-management.md`
- Q-852: `20-features/settings/sync-configuration.md`
