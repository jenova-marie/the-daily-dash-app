# User Journeys — the prototype as the account owner meets it

**Area:** overview · **Level:** 0 · **Status:** draft

**Tag summary:** Implemented 96 · Described 22 · Partial 5

**Sources owned:** none (synthesis). Every step cites the spec section or rule that governs it; the owning spec holds the full behaviour and the citations into source.

Six journeys, each told as the observed sequence of screens and writes. Where the manual or a walkthrough promises a step the code lacks, the step is kept in the narrative, tagged `[Described]`, and the discrepancy id is given. Vocabulary follows `00-overview/glossary.md`.

## A. First run — from the landing page to the Dashboard

1. **Landing.** The visitor opens `/` and sees the brand "The Daily Dash", the eyebrow "Your personal command center", the hero "Organize your day. Master your life.", eight feature cards, and three calls to action that all lead to `/auth` `[Implemented]` `20-features/app-shell/spec.md` §0.3, BR-SHELL-16 (copy in `product-vision.md` §1.2).
2. **Sign-up form.** On `/auth` the visitor switches to "Create your account" and fills "Full Name", "Email", "Password"; any empty field shows "Please fill in all fields" `[Implemented]` `10-architecture/auth-and-account.md` §2.
3. **Google or independent.** Submitting opens "How would you like to get started?" with "Connect Google Account — Sync Google Calendar events and Google Tasks automatically on app load." and "Use Independently — Manage tasks, schedules, and goals without connecting a Google account."; the footer says Google can be connected later in Settings `[Implemented]` `10-architecture/auth-and-account.md` AR-AUTH-01. The "on app load" sync does not exist; nothing syncs at launch `[Described]` `src/pages/Auth.jsx:314` (D-217, AR-SYNC-14).
4. **Verification code.** Either choice registers the account and switches to "Verify your email" with "Verification Code" and a "Resend" link; an accepted code signs the account in `[Implemented]` `10-architecture/auth-and-account.md` §2 "Verify mode", AR-AUTH-02.
5. **Google popups (Connect only).** After verification the Google Calendar authorisation popup opens, then, when it closes, the Google Tasks popup `[Implemented]` `10-architecture/auth-and-account.md` AR-AUTH-02, `10-architecture/google-sync.md` AR-SYNC-13. The connector ids used here differ from those Settings later probes `[Implemented]` `10-architecture/google-sync.md` §2a (D-200, Q-200).
6. **Terms gate.** The account lands on `/accept-terms`: "Welcome to The Daily Dash" with two scrollable summaries and two checkboxes, "I agree to the Privacy Policy" and "I agree to the Terms of Use"; "Continue to Dashboard" enables only when both are ticked `[Implemented]` `10-architecture/auth-and-account.md` AR-AUTH-03 (text in `00-overview/legal-copy.md` §4). The summaries omit sections of the full texts and the gate does not link to them `[Described]` `00-overview/legal-copy.md` §4 "What the summaries leave out" (D-970, Q-971).
7. **What accepting writes.** Two sign-up-era connectors are disconnected, `terms_accepted` and `privacy_accepted` are set, the collage seeding function runs, and the app navigates to `/dashboard` `[Implemented]` `10-architecture/auth-and-account.md` AR-AUTH-04.
8. **Seeded collage.** The seeding copies every admin-flagged default image into the new account only when it has none; the same call repeats on every shell mount and daily by workflow `[Implemented]` `10-architecture/admin-operations.md` AR-ADMIN-18..21, `20-features/vision-board/collage.md` BR-VB-COL-12, `10-architecture/automations.md` AR-AUTO-09, AR-AUTO-10.
9. **Terms re-check.** Every later mount of the signed-in shell re-reads the two flags and sends the account back to `/accept-terms` if either is missing `[Implemented]` `20-features/app-shell/spec.md` BR-SHELL-05.
10. **Dark by default.** The first paint is dark with a built-in or cached background; the saved theme is applied after sign-in `[Implemented]` `20-features/app-shell/spec.md` BR-SHELL-10..12.
11. **Dashboard walkthrough.** With no dismissal flag on the device, "Welcome to Dashboard" opens with four steps ("Your Daily Hub", "Arrange Your Widgets", "Launch Your Vision Slideshow", "Track Progress in Real-Time") and one button "Got it — Don't Remind Me Again" `[Implemented]` `20-features/dashboard/spec.md` §9, `20-features/onboarding/spec.md` BR-ONB-06. The dismissal is written to the account and the device, but only the device is consulted next time `[Implemented]` (D-961).
12. **Empty Dashboard.** The greeting reads "Good Morning" with the first word of the full name; widgets show their own empty states: "Complete a daily evaluation to see focal areas.", "All done for today!", "No items scheduled for today. Add items from Tasks, Education, or Calendar.", "No pending tasks for today", "No meals planned this week", "No goals", and the quote widget fetches today's quote `[Implemented]` `20-features/dashboard/spec.md` BR-DASH-02; `20-features/vision-board/spec.md` §4c; `20-features/daily-checklist/spec.md` §4c; `20-features/daily-schedule/spec.md` §4c; `20-features/tasks/spec.md` §4c; `20-features/chores/meal-planning.md` §4c; `20-features/goals/spec.md` §4c; `10-architecture/automations.md` §4.
13. **First Vision Board visit.** Opening `/visionboard` seeds thirteen pillars in Maslow order and five activities per pillar, then opens "Welcome to Your Vision Board 🌟" and lands on the Pillars tab `[Implemented]` `20-features/vision-board/spec.md` BR-VB-01, BR-VB-07, `health-pillars.md` BR-VB-PIL-04, `20-features/onboarding/spec.md` BR-ONB-08.
14. **Every other page** opens its own "Welcome to {Page}" once; a "Guide" button brings it back `[Implemented]` `20-features/onboarding/spec.md` BR-ONB-01, §4.3.

## B. A weekday — morning to midnight

### B.1 Morning on the Dashboard

1. **Greeting and weather.** "Good {Morning|Afternoon|Evening}, {name}" by device hour; the Weather widget uses a result cached within 30 minutes or asks the browser for its position and shows current conditions, today's high and low, air quality and a six-day strip `[Implemented]` `20-features/dashboard/spec.md` BR-DASH-01, BR-DASH-02, `20-features/weather/spec.md` BR-WX-01..03, BR-WX-09, BR-WX-10. The manual's "on first load" differs: the prompt recurs whenever the cache has expired `[Described]` `src/pages/UserManual.jsx:56` (D-810).
2. **Focal areas.** The widget lists the three lowest ratings of the most recent evaluation, "Grateful for" today's gratitude when one exists, and its header button is blue until today's evaluation exists; pressing it opens `/visionboard?tab=evaluation` `[Implemented]` `20-features/vision-board/spec.md` BR-VB-REM-01, BR-VB-REM-02, `20-features/dashboard/spec.md` BR-DASH-08.
3. **Checklist.** The Daily Checklist widget lists only items not ticked today, grouped Morning / Afternoon / Evening / Anytime, each with its `n/7` weekly count; ticking creates or flips today's completion `[Implemented]` `20-features/daily-checklist/spec.md` BR-CHK-08, BR-CHK-14, BR-CHK-16.
4. **Quote.** The widget reads today's daily quote or asks the backend for one; "New Quote" discards today's row and creates another; "Favorite" marks it `[Implemented]` `20-features/quotes/spec.md` BR-QUOTE-01, BR-QUOTE-07, BR-QUOTE-13; the quote was usually pre-created at 07:00 UTC `[Implemented]` `10-architecture/automations.md` AR-AUTO-05, AR-AUTO-06 (D-213).
5. **Today's Schedule.** Only calendar and event rows for today, sorted by start time `[Implemented]` `20-features/daily-schedule/spec.md` BR-SCHED-17.
6. **Today's Tasks.** Overdue first, then due today, bucketed by time of day when any has a due time; ticking flips status only `[Implemented]` `20-features/tasks/spec.md` §4.9 (D-460).
7. **Badges.** The Chores and Education buttons in that widget's header count due-today-or-overdue items and colour purple, red, blue, or dimmed; each opens `/chores?filter=due` or `/education?filter=due` `[Implemented]` `20-features/dashboard/spec.md` BR-DASH-05..07 (D-103..D-106).
8. **Menu & Chores.** The rest of the week's menu from today to Sunday, then "Chores Due Today" by member and room with a checkbox that completes and advances the next due date `[Implemented]` `20-features/chores/meal-planning.md` §4 "Dashboard", BR-MEAL-06, BR-MEAL-10.

### B.2 Time-blocking the day on the Daily Schedule

9. **Open on today.** The page loads the selected date's schedule items, the previous day's for carry-over, tasks, chores, goals, milestone tasks and due activities, and draws an hour grid from the device-local active hours (default 5 to 22) `[Implemented]` `20-features/daily-schedule/spec.md` BR-SCHED-01, §4 "Active hours", `time-grid.md`.
10. **Item library.** The side panel offers Recent entries (pinned first, plus "Chores" and "Edu" quick entries), the Tasks tab (undated or due-on-or-before-today tasks not yet placed today, grouped by label with an urgency badge), and the Goals tab (the oldest unscheduled milestone task per goal, or the goal itself) `[Implemented]` `20-features/daily-schedule/item-library.md` §2a..2c, BR-SCHED-64..70. The manual's "Already-scheduled items are highlighted" differs: placed items leave the library for that day `[Described]` `src/pages/UserManual.jsx:168` (D-403).
11. **Drop onto the grid.** The row's mini form takes a start time, a duration and (for custom entries) a priority; adding writes a schedule item for the date and, for a task, stamps the task's due date and schedule time `[Implemented]` `20-features/daily-schedule/item-library.md` BR-SCHED-76..79. The manual's "+" for a custom time block with colour and notes differs: the "+" is a Quick Task by title `[Described]` `src/pages/UserManual.jsx:167` (D-401, BR-SCHED-60).
12. **Auto-population.** The manual says calendar events, tasks with a scheduled time, education activities and chores due that day appear automatically; calendar rows and placed tasks do, while no path creates chore or education schedule items `[Described]` `src/pages/UserManual.jsx:166` (BR-SCHED-21, D-206, Q-203).
13. **Custom blocks reach Google.** A custom block created from the library is pushed to Google Calendar by the create workflow when the Calendar connector is held; edits and deletion follow once the Google id is written back `[Implemented]` `10-architecture/google-sync.md` AR-SYNC-21..23, `10-architecture/automations.md` AR-AUTO-17..20.
14. **The daily to-do.** The TO DO card lists the day's schedule items plus a synthetic row for every task due today, sorted by start time `[Implemented]` `20-features/daily-schedule/daily-todo.md` BR-TODO-01..03.
15. **Check-off writes through.** Ticking a to-do row marks the source task, chore, or education activity complete, then marks the schedule item complete and hides it from the grid; unticking restores both `[Implemented]` `20-features/daily-schedule/daily-todo.md` BR-TODO-05, `10-architecture/schedule-hub.md` AR-HUB-23, AR-HUB-24. The manual and walkthrough place a checkbox on the grid block itself; the grid has none `[Described]` `src/pages/UserManual.jsx:170` (D-402).
16. **Removing without deleting.** Swipe or hover reveals "Remove", and the dialog offers "Send to Item Library", "Keep in Calendar", "Hide from Schedule Grid", "Delete from App", "Delete from Google" or "Delete Permanently" by source type `[Implemented]` `20-features/daily-schedule/daily-todo.md` §5, BR-TODO-06..12 (`constitution.md` PR-03).
17. **Condensed checklist and menu.** The right column carries the Daily Checklist for the selected date and the Menu & Chores card `[Implemented]` `20-features/daily-schedule/daily-todo.md` §11, `20-features/chores/meal-planning.md` §0.
18. **Catching up.** A block whose time has passed offers "↻ Move to now"; hidden blocks sit in a hidden panel with "Restore to grid"; the eye toggle shows completed blocks `[Implemented]` `10-architecture/schedule-hub.md` AR-HUB-07, AR-HUB-10, AR-HUB-12.

### B.3 Tasks, chores, meals, and lessons during the day

19. **Tasks page.** Opens on the Active view grouped by priority, collapsed by default per the device setting; ticking a task sets its last-completed date, cascades to linked schedule items, and creates the next occurrence of a recurring task `[Implemented]` `20-features/tasks/spec.md` §4.3, §4.8, BR-TASK-14, `recurrence.md` §3. The manual's status circle with "In Progress" and keyword search are not present `[Described]` `src/pages/UserManual.jsx:108,127` (D-453, D-458).
20. **Chores page.** Opens on "Due Today"; ticking marks the chore complete, stamps today, and sets the next due date from the frequency; at local midnight completed chores return to pending while the page is open `[Implemented]` `20-features/chores/spec.md` BR-CHORE-02, BR-CHORE-03, BR-CHORE-13, BR-CHORE-15. "Overdue chores are automatically highlighted" has no counterpart on this page `[Described]` `src/pages/UserManual.jsx:194` (BR-CHORE-16, D-104).
21. **Menu tab.** Today's row is expanded and marked "today"; meals are grouped by slot; ticking a meal completes it and double-tapping opens its notes and directions `[Implemented]` `20-features/chores/meal-planning.md` §4 "Chores page — Menu tab", BR-MEAL-04, BR-MEAL-05. Meals cannot be edited after creation `[Implemented]` (Q-600).
22. **Education page.** Opens on "Due" with one subject card per learner and subject that has a passing activity; ticking a repeating activity records today and moves its due date forward; a one-time activity moves to "Done" `[Implemented]` `20-features/education/spec.md` §4.10, BR-EDU-09, BR-EDU-10, §5b. "Add to Schedule" is promised and has no creating path `[Described]` `src/pages/UserManual.jsx:249` (D-206).
23. **Live refresh.** The Daily Schedule, to-do, Calendar, Dashboard goals and menu widgets subscribe to entity changes and reload themselves; the Tasks, Chores, Education and Goals pages reload after their own writes `[Implemented]` `10-architecture/shared-interactions.md` AR-UI-14, `10-architecture/schedule-hub.md` AR-HUB-11, `20-features/chores/spec.md` §4.11, `20-features/education/spec.md` BR-EDU-30.

### B.4 Evening — evaluation, reflection, slideshow

24. **Daily evaluation.** From the Dashboard's target button or the Vision Board's "Daily Eval" tab, the wizard shows one step per visible pillar ("Step n of total"); each step takes a 1–5 rating, optional "Activities Used" ticks, and notes `[Implemented]` `20-features/vision-board/daily-evaluation.md` BR-VB-EVAL-03..05.
25. **Queued goals.** A rating of 3 or below opens "Suggested Goals"; "+ Goal" on an activity takes an occurrence count and a frequency and "Add to Plan" queues it in memory; nothing is written yet `[Implemented]` `20-features/vision-board/daily-evaluation.md` BR-VB-EVAL-06, BR-VB-EVAL-07. Activities that already have an active goal show "{n} left" `[Implemented]` BR-VB-EVAL-08.
26. **Gratitude and Complete.** The last step asks "What is one thing you are grateful for today?" and lists "Goals to be created on completion"; Complete writes one tracking row per rated pillar, one gratitude row, one goal plus its milestone tasks per queued activity, then switches to the Pillars tab `[Implemented]` `20-features/vision-board/daily-evaluation.md` BR-VB-EVAL-09..11, `20-features/vision-board/spec.md` BR-VB-05.
27. **Reflection.** On `/quotes` the account owner writes a reflection under today's quote and saves it to the row; favourites and the last 50 quotes are listed below `[Implemented]` `20-features/quotes/spec.md` BR-QUOTE-06, BR-QUOTE-10.
28. **Slideshow.** "Vision" on the Dashboard, or the Collage tab's buttons, opens the full-screen player: Auto-Generated asks the model for three affirmations per pillar rated 3 or below in the most recent evaluation, interleaved round-robin; Custom plays every saved affirmation over the chosen images; ambient audio and optional spoken affirmations follow device-local preferences `[Implemented]` `20-features/vision-board/slideshow.md` BR-VB-SLIDE-01, BR-VB-SLIDE-02, BR-VB-SLIDE-09, BR-VB-SLIDE-11. From the Dashboard, Custom shows no image picker `[Implemented]` (D-751).
29. **Midnight.** Nothing is reset: the checklist reads the new date's completions, chores flip back to pending while the Chores page is open, the Quotes page reloads when the local date rolls over, and the new day's quote is pre-created at 07:00 UTC `[Implemented]` `20-features/daily-checklist/spec.md` BR-CHK-09, `20-features/chores/spec.md` BR-CHORE-15, `20-features/quotes/spec.md` BR-QUOTE-05, BR-QUOTE-14 (`constitution.md` PR-11).

## C. A week — review, progress, and paper

1. **Weekly review.** The Vision Board's "Weekly Review" tab shows the Sunday-to-Saturday week: "Focal Areas (Lowest Averages)" over This Week, 3 Months or All Time; a seven-day chart with pillar filter chips and a hover panel; average buckets 5 down to 1; and a Daily Breakdown with notes `[Implemented]` `20-features/vision-board/weekly-review.md` BR-VB-WK-01..07, §8.
2. **Checklist consistency.** Each active checklist item shows how many of the seven days it was ticked, with a colour band by count; the same `n/7` appears on the checklist page and widget and reloads at Sunday 00:00 `[Implemented]` `20-features/vision-board/weekly-review.md` BR-VB-WK-08, BR-VB-WK-09, `20-features/daily-checklist/spec.md` BR-CHK-14, BR-CHK-15.
3. **Goal progress.** Ticking milestone tasks on the Goals page derives the goal's progress, status, archive flag and timestamps; completing the last milestone archives the goal; "Restore" returns it to In Progress `[Implemented]` `20-features/goals/milestone-tasks.md` BR-GOAL-T05..T07, `20-features/goals/spec.md` BR-GOAL-10. The Dashboard "Goals Overview" lists non-completed goals with a "✓" that completes without touching milestones `[Implemented]` BR-GOAL-14, BR-GOAL-15 (D-004). The manual's progress slider and "On Hold" are not present `[Described]` `src/pages/UserManual.jsx:287-288` (D-609, D-610).
4. **Printing the chore sheet.** "Print chores" on the Chores tab asks "Add a Note to Print", remembers the note on the device, and prints "Weekly Chore Schedule": one section per member, sub-grouped Daily / Weekly / Bi-Weekly / Monthly, rows with checkboxes `[Implemented]` `10-architecture/export-print-email.md` §3a, §4b. "Email chores" builds a weekday-first sheet with daily and weekly chores only `[Implemented]` (D-312).
5. **Printing the menu.** "Print meal schedule" on the Menu tab prints "Weekly Meal Schedule" by weekday for chores typed exactly `Meal` `[Implemented]` `20-features/chores/meal-planning.md` BR-MEAL-12 (D-603).
6. **Printing the homeschool sheet.** "Print education schedule" and "Email education schedule" (shown while a learner is active) produce "Weekly Education Schedule" with Learning Plans, Daily, Weekly (by weekday), One-Time and Other activities, each with a checkbox, honouring the status view and subject filter `[Implemented]` `20-features/education/spec.md` §4.13, BR-EDU-25, `10-architecture/export-print-email.md` §3b.
7. **Other sheets.** The Tasks page prints the active filter grouped by priority or label; the Calendar and Dashboard widgets ask for a date range; the Daily Schedule asks whether to include the to-do; Goals print per timeframe card; the Weekly Review prints as rendered `[Implemented]` `10-architecture/export-print-email.md` §3, §4a, §4c, `20-features/vision-board/weekly-review.md` §9.
8. **Email means self.** Every email export goes to the signed-in address and ends with "Sent to your email!" `[Implemented]` `10-architecture/export-print-email.md` AR-EXPORT-01, AR-EXPORT-03.

## D. Household admin — members, learners, libraries, links, theme

1. **Household members.** On the Chores page the "Manage household members" button opens "Household Members": "Add Person" with a name (default colour), edit name and colour by clicking, delete through the shared confirm. The Goals page's "Manage Family Members" lists and creates the same records by name only `[Implemented]` `20-features/chores/spec.md` §4.1, BR-CHORE-23, `20-features/goals/spec.md` §4 "Household members". A chore cannot be created until one member exists: "Please add a household member first before creating chores." `[Implemented]` BR-CHORE-09. The manual's "Members" tab is a dialog `[Described]` `src/pages/UserManual.jsx:192` (D-550).
2. **Learners.** "Manage Learners" on the Education page takes a name and grade level; deleting a learner deletes that learner's plans and activities `[Implemented]` `20-features/education/spec.md` §4.2, BR-EDU-05. The manual's learner colour has no input `[Described]` `src/pages/UserManual.jsx:235` (D-654).
3. **Plans and activities.** "Edu Plan" creates one plan per selected subject (ten defaults plus device-stored custom subjects); each subject card takes assignments and activities with a frequency, days, due date, notes and resource links `[Implemented]` `20-features/education/spec.md` §4.3, §4.6, §4.9, BR-EDU-01, BR-EDU-03.
4. **Chore library.** The toolbar's "Chore Library" dialog merges saved templates with the owner's own chores ("My Chore"), takes default assignees with per-entry overrides, and "Assign n Chore(s) (m assignment(s))" creates one pending chore per entry and assignee `[Implemented]` `20-features/chores/chore-library.md` BR-CHORE-32..35. Templates are written from the add and edit dialogs' "Save to library" box and from the generator `[Implemented]` BR-CHORE-12, BR-CHORE-31.
5. **AI chore and meal generation.** The wand button opens "Generate Chores with AI": assignees, age group (`3-5` to `Adult`), type (Cleaning, Organizing, Maintenance, Meal, Other), room or meal type, quantity 1–50; the backend prompt asks for age-appropriate items; the review step selects, assigns, sets frequency and days, and "Add n Item(s)" or "Save to Library" writes them `[Implemented]` `10-architecture/ai-services.md` §2a..2d, `20-features/chores/ai-generator.md`. The manual's age groups and "Generate with AI" button label differ `[Described]` `src/pages/UserManual.jsx:202-204` (D-305, D-306).
6. **AI activity generation.** The wand button on Education opens "Add Activities" in AI mode: age group, subject, type, quantity 1–10; review selects learners and activities, then plans, and can also create weekly goals `[Implemented]` `10-architecture/ai-services.md` §3, `20-features/education/spec.md` BR-EDU-28. "Save to Activity Library" during review is promised and not offered there `[Described]` `src/pages/UserManual.jsx:257` (D-659).
7. **Link library.** The link-icon button opens "Manage Links & Categories": create categories with one of 42 icons and 10 colours (kept on the device), add links manually with an image or icon thumbnail, or import a browser bookmarks file under "Imported" `[Implemented]` `20-features/links/spec.md` BR-LINK-02, BR-LINK-08, BR-LINK-12. The manual's auto-fetched thumbnail and category tabs differ `[Described]` `src/pages/UserManual.jsx:350,352` (D-910, D-911).
8. **Theme.** The Theme Editor applies colours, dark mode, fonts, size, opacity, radius and background live; "Save Theme" writes them to the account; the background library holds up to 20 URLs and can randomise on every load `[Implemented]` `20-features/theme-editor/spec.md` BR-THEME-02, BR-THEME-03, BR-THEME-06, BR-THEME-10. The greeting name is set in Settings, not here `[Implemented]` BR-THEME-16 (D-920).
9. **Feature toggles and greeting.** Settings' Dashboard card flips Vision Board, Education and Chores on or off (saved on click, removing the sidebar entry live) and saves a "Custom Display Name" for the greeting `[Implemented]` `20-features/settings/spec.md` BR-SET-02, BR-SET-03, `10-architecture/preferences.md` AR-PREF-21..23. Widgets stay on the Dashboard regardless `[Implemented]` `20-features/dashboard/spec.md` BR-DASH-09; the manual says they hide `[Described]` `src/pages/UserManual.jsx:34` (D-120).
10. **Dashboard order.** Reorder mode lets the owner drag the eight widgets; each drop is saved to the account `[Implemented]` `20-features/dashboard/spec.md` BR-DASH-04.

## E. A Google user — connect, scope, import, push, delete, disconnect

1. **Connect.** Settings → Integrations probes both connectors and shows "Connect"; pressing it opens the authorisation popup, polls up to ten times after it closes, and shows "Google Calendar connected!" / "Before syncing, scroll down to "Select Calendars to Sync" …" (or the Tasks banner) `[Implemented]` `10-architecture/google-sync.md` AR-SYNC-04, AR-SYNC-05, `20-features/settings/spec.md` BR-SET-06..08.
2. **Choose calendars.** "Fetch Calendars" creates one `SelectedCalendars` row per Google calendar, selecting only the primary; each checkbox writes immediately and never clobbers an earlier choice `[Implemented]` `10-architecture/google-sync.md` AR-SYNC-15, `20-features/settings/spec.md` BR-SET-11.
3. **Choose task lists.** "Load Lists" records each Google list; the checkboxes are stored and read by no import, which walk every list `[Partial]` `20-features/settings/spec.md` §4.7 (D-483, D-204).
4. **Choose sources and times.** The sync dialog's "📅 Google Calendar Events" / "✓ Google Tasks" boxes persist as sync sources and steer both the manual and the scheduled runs; "Scheduled sync times" are stored and unread, the workflow running once at 12:00 UTC `[Implemented]` BR-SET-14, `10-architecture/automations.md` AR-AUTO-13; `[Partial]` AR-AUTO-14 (D-204, Q-202). The manual's "Set Auto-Sync times" describes the stored list `[Described]` `src/pages/UserManual.jsx:422`.
5. **Import.** "Sync Selected" runs the calendar import (now − 90 to + 60 days, one page per calendar, dedup by Google id, deletion guards) then the tasks import (every list, upsert by Google task id, default label); the Calendar page's sync button does the same per the sources `[Implemented]` `10-architecture/google-sync.md` §4a, §5a, `20-features/calendar/spec.md` BR-CAL-12. The dialog closes when the first import ends `[Implemented]` (D-850). The manual's "past 30 days" is the scheduled import's lower bound `[Described]` `src/pages/UserManual.jsx:149,421` (D-201, D-500).
6. **Imported rows.** Google events become `calendar` schedule items on the Calendar page, grid, to-do and Dashboard; Google tasks become tasks labelled with the default sync label ("Google Tasks" when unset) `[Implemented]` `10-architecture/schedule-hub.md` §3b, `20-features/tasks/google-tasks.md` BR-TASK-41..43. No control sets that label `[Partial]` (D-222, D-481, Q-102).
7. **Push a custom block.** Placing a custom entry on the Daily Schedule creates a `custom` row; the create workflow posts it to the primary calendar and writes the Google ids back; later updates and deletions follow; imported rows are never echoed back `[Implemented]` `10-architecture/google-sync.md` AR-SYNC-21..24, `10-architecture/automations.md` §3.
8. **Push a Calendar-page event.** "Add Calendar Event" with "Sync to Google Calendar" ticked (default on) pushes on create; edits from the Calendar page push whenever the event is linked; edits from the schedule or to-do push when "Apply permanent changes to Google Calendar" is ticked `[Implemented]` `20-features/calendar/spec.md` BR-CAL-05, BR-CAL-14, US-CAL-03.
9. **Push a task due date.** The Edit Task dialog's "Apply permanent changes to Google Tasks" sends the new due date and time to Google's default list; nothing else is pushed for tasks `[Implemented]` `20-features/tasks/google-tasks.md` BR-TASK-45, BR-TASK-46 (D-484).
10. **Delete here or from Google.** On the Calendar page a linked event asks "Delete here only" or "Delete from Google too"; a `calendar` row is removed from app and stays for dedup `[Implemented]` `20-features/calendar/spec.md` BR-CAL-02..04, `10-architecture/schedule-hub.md` AR-HUB-14..16. On the to-do, "Delete from Google" deletes the Google task or event through the backend `[Implemented]` `20-features/daily-schedule/daily-todo.md` BR-TODO-11. On the Tasks page "Delete from Google too" runs an import instead of a delete `[Implemented]` `20-features/tasks/google-tasks.md` §4 (D-485, Q-460).
11. **Google-side deletions.** A vanished Google event is deleted locally only by the manual import, only inside its window, only when its calendar fetched cleanly and the row carries both Google ids; Google Tasks deletions are never propagated `[Implemented]` `10-architecture/google-sync.md` AR-SYNC-31..36.
12. **Reviewing tombstones.** The Calendar page's "Items Deleted from Google Calendar" panel offers "Keep deleted (don't restore)" and "Restore to calendar" per tombstone; a restore lands on the current date at 09:00–10:00 `[Implemented]` `20-features/calendar/deleted-item-review.md` §5, BR-CAL-22..24. No code creates a tombstone `[Partial]` §7 (D-211, Q-500). The manual's "review and manage deleted sync items" describes this panel `[Described]` `src/pages/UserManual.jsx:436`.
13. **Bookkeeping.** "Account:" and "Last sync:" appear under the Google Calendar card from the sync-state row; each calendar shows "Last synced:" `[Implemented]` `10-architecture/google-sync.md` AR-SYNC-45, AR-SYNC-46 (D-216).
14. **Disconnect.** "Connected" opens "Disconnect Google Calendar?" — "This will remove the connection. You can reconnect anytime. Your Google data will not be affected."; only the connector is revoked `[Implemented]` `10-architecture/google-sync.md` AR-SYNC-08, `20-features/settings/spec.md` BR-SET-09.
15. **Wipe synced data.** "Delete Synced Data" removes every schedule item (of any source type), the calendar selections, the sync state, and every task with a Google task id, sends nothing to Google, then disconnects every connected connector `[Implemented]` `20-features/settings/data-management.md` §2, BR-SET-52, BR-SET-53, BR-SET-55 (D-853, Q-851). Re-syncing means connect, fetch, re-tick, sync `[Implemented]` `data-management.md` §4.

## F. Leaving — trash, wipe, delete account

1. **Trash.** A task deleted from its row on the Tasks page is snapshotted first; Settings → Trash Bin lists snapshots from the last 24 hours ("Items in trash can be restored for 24 hours") with "Restore", which creates a new task from every field except the id, and a trash button that discards the snapshot; neither confirms `[Implemented]` `20-features/settings/trash-bin.md` §2, §3, BR-SET-40..43. The manual's "within 30 days" differs `[Described]` `src/pages/UserManual.jsx:111` (D-486, Q-462). Only tasks, and only single-row deletes, enter the trash `[Implemented]` `trash-bin.md` §2.
2. **Delete all app data.** "Delete All App Data" → "Delete Everything" walks twenty-five entities (tasks, schedule, chores, members, goals, checklist, education, quotes, links, pillars, ratings, affirmations, gratitude, reminders, theme, collage, sync state and selections, tombstones), disconnects the connectors, and leaves the account signed in with the trash, chore library, activity library, private uploads and every device-local preference intact `[Implemented]` `20-features/settings/data-management.md` §2, BR-SET-54, `10-architecture/admin-operations.md` AR-ADMIN-28 (D-855).
3. **Delete account.** "Delete Account" → "Delete Account?" — "This will permanently delete your account and all your data including tasks, schedules, goals, chores, and education plans. This action cannot be undone." The backend revokes six connectors, deletes sixteen entity types, and deletes the user; the client signs out and lands on `/auth` `[Implemented]` `10-architecture/auth-and-account.md` AR-AUTH-09, `20-features/settings/spec.md` BR-SET-20. Pillar ratings, gratitude, affirmations, collage rows, the trash and both libraries are outside the list `[Implemented]` `10-architecture/admin-operations.md` AR-ADMIN-35 (D-333, D-214, Q-333, Q-211).
4. **Signing out.** No sidebar, header or Settings control signs the account out; account deletion and token expiry are the only exits `[Partial]` `10-architecture/auth-and-account.md` AR-AUTH-08 (D-340, Q-331).
5. **Password.** "Change Password" in Settings changes it inline with the current password; the "Forgot password?" link on the sign-in screen emails a reset link `[Implemented]` `10-architecture/auth-and-account.md` §4, §10 (D-332, D-336).

## Discrepancies & open questions

No new ids are opened here. Cited: D-004, D-103..D-106, D-120, D-200, D-201, D-204, D-206, D-211, D-213, D-214, D-216, D-217, D-222, D-305, D-306, D-312, D-332, D-333, D-336, D-340, D-401..D-403, D-453, D-458, D-460, D-481, D-483..D-486, D-500, D-550, D-603, D-609, D-610, D-654, D-659, D-751, D-810, D-850, D-853, D-855, D-910, D-911, D-920, D-961, D-970, D-971; Q-102, Q-200, Q-202, Q-203, Q-211, Q-331, Q-333, Q-460, Q-462, Q-500, Q-600, Q-851, Q-971.
